This challenge explores zip/tar-related vulnerabilities; the server `automatically extracts` every zip/tar file you upload. Could you exfiltrate the flag?

Ps. These vulnerabilities have been patched by default on Python 3.12+ unless you explicitly tell the extracting libraries to trust the files fully.
